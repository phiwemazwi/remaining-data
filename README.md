# remaining-data
A backend system that exposes an API to return a subscribers remaining data balance. In other words "Give me the list of bundle balances for this number (MSISDN), mirroring a real enterprise telacom system.
